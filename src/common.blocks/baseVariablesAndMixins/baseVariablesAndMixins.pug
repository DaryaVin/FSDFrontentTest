mixin linearGradientForSVG(color)
  linearGradient(id=color+"LinearGradient", x1="50%", y1="0%", x2="50%", y2="100%")
    stop(offset="0%", stop-color="grey")
    stop(offset="100%", stop-color="grey")

mixin addBlock(key)
  - blocks[key] = this.block
